[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --ignore=layers
    --ignore=node_modules
    --ignore=__pycache__
    --tb=short
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
